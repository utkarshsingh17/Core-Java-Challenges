Answer 1-

JRE stands for Java Runtime Environment is a software tools used for developing Java
applications and to provide the runtime environment.

JVM satnds for Java Virtual Machine which provides a runtime environment in which Java
bytecode can be executed.

JDK stands for Java Development Kit is asoftware development environment which is used to
develop Java application.

Answer 2-

The result of the javac compiler is the .class file or the bytecode and can be executed 
on all kinds of operating systems.The byte code generated by java needs JVM only.It does
not depend on the operating system.

Answer 3-

Output-
2
3
4

Explaination-

i=0
	j=0+1
	if(myArray[0]==myArray[1])--False

	j=2
	Condition--False
For i=0(myArray[0]) which is 1 array does not contain any other 1 and inner loop is 
traversing a step ahead of outer loop so for i=0 nothing will be printed

i=1
For i=1(myArray[1]) which is 2 the inner loop will get executed and 2 will be printed 
as 2 is present  after  index 1 at index 7 .

i=2
For i=2(myArary[2]) which is 3 the inner loop will get executed a head of innner loop
and 3 will be printed as 3 is present after index 2 at index 8.

i=3
For i=3(myArray[3]) which is 4 the inner loop will get executed a head of inner loop and
4 will be printed as it is present after index 3 at index 9.

After index 3 nothing will be printed as array doesnot contain duplicates of those elements.

Answer 4-

import java.util.*;

class question4 {
    public static void main(String args[]) {
        int l, w, a, p;
        Scanner sc = new Scanner(System.in);
        System.out.println("Input the length of rectangle:");
        l = sc.nextInt();
        System.out.println("Input the width of rectangle:");
        w = sc.nextInt();
        p = 2 * (l + w);
        a = l * w;
        System.out.println("Area of rectangle is:" + a);
        System.out.println("Perimeter of the rectangle:" + p);

    }
}

Answer 5-

import java.util.*;

public class question5 {
    public static void main(String args[]) {
        int d;
        double a;
        Scanner sc = new Scanner(System.in);
        System.out.println("Input the diameter:");
        d = sc.nextInt();
        d = d / 2;
        a = d * d * (3.14);
        System.out.println(a);

    }

}
 
Answer 6-

import java.util.*;

public class question6 {
    public static void main(String args[]) {
        int a, b, c, t;
        double per;
        Scanner sc = new Scanner(System.in);
        System.out.println("Marks of subject A:");
        a = sc.nextInt();
        System.out.println("Marks of subject B:");
        b = sc.nextInt();
        System.out.println("Marks of subject c:");
        c = sc.nextInt();
        t = a + b + c;
        per = (double) t / 3;
        System.out.println("Total marks:" + t);
        System.out.println("Percentage:" + String.format("%.2f", per) + "%");

    }

}

Answer 7-

import java.util.*;

public class question7 {
    public static void main(String args[]) {
        int a, b, c;
        Scanner sc = new Scanner(System.in);
        System.out.println("Value of a:");
        a = sc.nextInt();
        System.out.println("Value of b:");
        b = sc.nextInt();
        c = a;
        a = b;
        b = c;
        System.out.println("Value of a:" + a);
        System.out.println("Value of b:" + b);

    }

}

Answer 8-

import java.util.*;

public class question8 {
    public static void main(String args[]) {
        System.out.println(-5 + 8 * 6);
        System.out.println((55 + 9) % 9);
        System.out.println(20 + -3 * 5 / 8);
        System.out.println(5 + 15 / 3 * 2 - 8 % 3);
    }

}

Answer 9-

import java.util.*;

public class question9 {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Insert the value of a:");
        int a = sc.nextInt();
        System.out.println("Insert the value of b:");
        int b = sc.nextInt();
        System.out.println((int) Math.pow(7, 5));

    }

}

Answer 10-
import java.util.*;

public class question10 {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Insert the value in Fahrenheit:");
        int f = sc.nextInt();
        int c = ((f - 32) * 5) / 9;
        System.out.println(c);

    }

}
